<Window x:Class="Version_2._0.View.CreateWork"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Version_2._0.View"
        xmlns:btn="clr-namespace:Version_2._0.View.Button"
        mc:Ignorable="d"
        Title="CreateWork" Height="450" Width="800">
    <Grid>
        <Grid>
            <Grid Background="White" Opacity=".75" Panel.ZIndex="10" />
            <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Panel.ZIndex="11" Width="350">
                <Border Padding="16" CornerRadius="16">
                    <StackPanel>
                        <TextBlock
                            TextAlignment="Center"
                            FontSize="20"
                            FontWeight="Bold"
                            Margin="0,0,0,10"
                            Text="Create / Update a backup work" />

                        <TextBlock
                            Visibility="Collapsed"
                            TextWrapping="Wrap"
                            x:Name="ErrorMessageTextBlock"
                            Margin="0,0,0,16"
                            Foreground="Red" />

                        <TextBlock Margin="0,0,0,4" Text="Name:" />
                        <TextBox x:Name="JobNameTextBox"
                                 Margin="0,0,0,16"/>

                        <TextBlock Margin="0,0,0,4" Text="Source of the folder:" />
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="SourcePathTextBox" Grid.Column="0" />
                            <Button Grid.Column="1" Content="..." Width="32" Margin="4,0,0,0"/>
                        </Grid>

                        <TextBlock Margin="0,0,0,4" Text="Target:" />
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="TargetPathTextBox" Grid.Column="0" />
                            <Button Grid.Column="1" Content="..." Width="32" Margin="4,0,0,0"/>
                        </Grid>

                        <TextBlock Margin="0,0,0,4" Text="Type of work:" />
                        <ComboBox Margin="0,0,0,16" x:Name="JobTypeComboBox">
                            <ComboBoxItem Content="Complete" />
                            <ComboBoxItem Content="Differential" />
                        </ComboBox>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <btn:CancelButton Grid.Column="1" Margin="0,0,8,0"/>
                            <local:ConfirmButton Grid.Column="0" Margin="0,0,8,0"/>
                        </Grid>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>
